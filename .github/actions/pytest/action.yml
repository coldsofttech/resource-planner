name: Pytest
description: Run pytest (unit + integration) and upload the report

inputs:
  path:
    description: Path to the tests directory
    required: true
  name:
    description: Display name used in report filenames
    required: true
  extra-args:
    description: Additional pytest arguments (e.g. -p no:django --import-mode=importlib)
    required: false
    default: ""
  artifact-name:
    description: Name of the uploaded artifact
    required: true

runs:
  using: composite
  steps:
    - name: pytest (${{ inputs.name }})
      shell: bash
      run: |
        mkdir -p .pytest-reports
        set +e
        pytest --override-ini=addopts= -n auto --dist=loadfile ${{ inputs.extra-args }} \
          --junit-xml=.pytest-reports/junit-${{ inputs.name }}.xml \
          --html=.pytest-reports/test-report-${{ inputs.name }}.html \
          --self-contained-html \
          ${{ inputs.path }}
        EXIT_CODE=$?
        set -e
        if [ ! -f .pytest-reports/junit-${{ inputs.name }}.xml ]; then
          printf '<?xml version="1.0" encoding="utf-8"?>\n<testsuites/>\n' > .pytest-reports/junit-${{ inputs.name }}.xml
        fi
        if [ ! -f .pytest-reports/test-report-${{ inputs.name }}.html ]; then
          printf '<!DOCTYPE html><html><body><p>No test report generated for ${{ inputs.name }}.</p></body></html>\n' > .pytest-reports/test-report-${{ inputs.name }}.html
        fi
        if [ $EXIT_CODE -ne 0 ] && [ $EXIT_CODE -ne 5 ]; then
          exit $EXIT_CODE
        fi

    - name: Upload test report
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: ${{ inputs.artifact-name }}
        path: .pytest-reports/
